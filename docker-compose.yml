services:
  db:
    image: postgres:15
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=sampleton_db
      - POSTGRES_USER=usuario
      - POSTGRES_PASSWORD=password123
    ports:
      - "5432:5432"

volumes:
  postgres_data: